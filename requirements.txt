numpy>=1.24
pandas>=2.0
openpyxl>=3.1
# Optional: enables the FOCuS structural-break detector. Without it, the
# regime layer silently falls back to CUSUM-only signals.
changepoint_online>=0.2

# Sybilion API client
sybilion

# Core dependencies
scipy>=1.10.0

# Optional: for meta-model stretch goal
# scikit-learn>=1.3.0
# xgboost>=2.0.0

# Testing
pytest>=8.0.0

# Backend API layer
fastapi
uvicorn
